##Opencv笔记
***
```c++
waitKey(int delay)   //当delay<=0时，函数无限等待键事件
//它返回所按下的键的代码，若未按下任何健返回-1
```

sudo apt-get install build-essential libgtk2.0-dev libavcodec-dev libavformat-dev libjpeg.dev libtiff5.dev libswscale-dev libjasper-dev  
