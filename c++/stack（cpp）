### stack
***

1. 栈：是基本的数据结构之以，特点是**先进后出**。

* 常用操作：
```
1. stack<int> q;
2. int x;
3. q.push(x);//压入栈顶
4. q.top();//返回栈顶元素
5. q.pop();//删除栈顶的元素
6. q.size();//返回栈中的元素个数
7. q.empty();//检查栈是否为空，若空返回true，否则返回false

```
