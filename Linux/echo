##echo 详解
**
1. 插入输出流
* echo [-neE][ARG...]
描述

        在标准输出上显示由单个空格字符分隔并后跟换行符的 ARG。
选项
1.         -n        不加换行符。
2.         -e        启用对后续反斜杠转义。
3.         -E        明确禁止反斜杠转义。

* echo -e "\e[背景颜色;文字颜色;显示方式m字符串\e[0m"

2. 覆盖文件内容
```
echo "dddd" > test.txt
echo "ddddd" > test.txt
```
